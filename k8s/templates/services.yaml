apiVersion: v1
kind: Service
metadata:
  name: {{ $.Chart.Name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app: {{ $.Chart.Name }}
    chart: {{ $.Chart.Name }}-{{ $.Chart.Version }}
    release: {{ .Release.Name }}
spec:
  clusterIP: None
  ports:
  - name: rsync
    port: 873
    protocol: TCP
    targetPort: 873
  selector:
    app: {{ $.Chart.Name }}
    release: {{ .Release.Name }}
  type: ClusterIP
